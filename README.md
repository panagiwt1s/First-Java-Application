# First-Java-Application 

A little Java algorithm on Eclipse IDE that generates a special matrix, which every line contains less/or equal number of 'ones' than the previous, and read the matrix in O(n) time. 

Base on a Data Structure exersice.
